# =============================================================================
# VM INVENTORY
# =============================================================================
# Add your GPU VMs here
# =============================================================================

all:
  children:
    gpu_vms:
      hosts:
        # =====================================================================
        # Add your VMs below. Example:
        #
        # gpu-vm-01:
        #   ansible_host: 10.0.1.101
        #   ansible_user: "{{ lookup('env', 'USER') }}"
        #   gpu_type: "Tesla V100"
        #   gpu_count: 4
        #
        # gpu-vm-02:
        #   ansible_host: 10.0.1.102
        #   gpu_type: "A100"
        #   gpu_count: 8
        # =====================================================================
        
        # Local testing
        localhost:
          ansible_host: 127.0.0.1
          ansible_connection: local
